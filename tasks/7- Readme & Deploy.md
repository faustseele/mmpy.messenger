**basics**
- api abouts
	- 'how to add page'
	- 'how to add component inside page' 
		- types (map, children-nodes) -> params (getNode + edge) -> component (get instance + {{{ render }}})
		- use as PageMap for TS access
- [ ] re-name & reduce yandex -> instead yandex-API
- [ ] description in EN | GER | RU

**specs**
- ex-tech: vite, ts, handlebars, xhr, post-css, css-modules, eslint, stylelint, testing, etc
- in-tech: factory-component + DI-services, store, event-bus, etc
- deploy: netlify

**arch**
- factory-component-based mini-framework
- feature-based structure (lightweight Feature‑Sliced Design)
- multi-nested components: AuthPage -> Button -> Spinner
- Flux Store

**design** ­– hand-made figma

**screenshots**

**Day 6: Docs/Deploy (5h)**  
- [ ] README framework flows/consistency, badges/GIFs.  
- [ ] GH Actions/Netlify/GH Pages.  
Commit: "docs deploy"

animations

